package com.betteropinions.catalogapplication.ui.theme

import androidx.compose.ui.graphics.Color

// ── Brand / Primary ─────────────────────────────────────────────────────────
val Purple = Color(0xFF5C2D91)          // primary brand purple (scaffold, app bars, buttons)
val PurpleLight = Color(0xFF6B3FA0)     // lighter purple (interactive: focus borders, icons, OTP timer)
val PurpleDeep = Color(0xFF1A0A2E)      // deep purple-black (paywall gradient mid)
val PurpleIndicator = Color(0xFFF3E8FF) // very light purple (nav bar indicator chip)

// ── Text ────────────────────────────────────────────────────────────────────
val NavyDark = Color(0xFF1A1A2E)        // dark navy (page titles, prefix text)
val BluePurpleDark = Color(0xFF2D2D4E)  // dark blue-purple (dialog heading/subtitle)

// ── Semantic ────────────────────────────────────────────────────────────────
val GreenSuccess = Color(0xFF4CAF50)    // green (check bullet icon, refund text)
val GreenLight = Color(0xFFE8F5E9)      // light green background (refund badge)

// ── Grays ───────────────────────────────────────────────────────────────────
val GrayLabel = Color(0xFF555555)       // medium-dark label (e.g. "Mobile Number")
val GraySubtitle = Color(0xFF666666)    // subtitle text
val GrayTimer = Color(0xFF888888)       // timer / secondary text
val GrayNav = Color(0xFF9E9E9E)         // unselected nav icon + label
val GrayPlaceholder = Color(0xFFAAAAAA) // text field placeholder
val GrayBorder = Color(0xFFCCCCCC)      // unfocused text field border (number input)
val GrayBorderLight = Color(0xFFDDDDDD) // unfocused text field border (OTP input)
val SlateGrayBlue = Color(0xFF677183) // unfocused text field border (OTP input)
val DarkSlateGrayBlue = Color(0xFF414D64) // unfocused text field border (OTP input)
val LavenderMist = Color(0xFFEFECF7) // unfocused text field border (OTP input)